#pragma once
#include <vector>
#include <string>

using namespace std;

int getRandomNumber(int);
string getRandomWord(const vector<string> &);
void displayWord(string &);
void playGame(const vector<string> &);
void clearScreen();

